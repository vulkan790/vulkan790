name: GitHub-Profile-Summary-Cards

on:
  create:
  schedule:
    - cron: "* */24 * * *"
  workflow_dispatch:

jobs:
build:
  runs-on: ubuntu-latest
  name: generate-github-cards
  permissions:
    contents: write

  steps:
    - uses: actions/checkout@v2
    - uses: vn7n24fzkq/github-profile-summary-cards@release
      env:
        GITHUB_TOKEN: ${{ secrets.[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] }}
      with:
        USERNAME: ${{ github.repository_owner }}
